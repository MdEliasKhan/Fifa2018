<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bespokesoftbd.eliaskhan.fifa2018">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/fifa_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fifa_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:name=".WelcomeScreen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".FixturesActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".GroupsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".TeamsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".PlayersDetailsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".WorldCupDetailsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".AboutThisFifaActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".StadiumActivity"></activity>
    </application>

</manifest>